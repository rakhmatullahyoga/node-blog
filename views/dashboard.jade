extends layout

block content
    .wrapper
      nav.nav
        a#logo(style='border:none;')
          h1
            | Node
            span -
            | Blog
        ul.nav-primary
          li
            a(href='/newpost') + Tambah Post
          li
            a(href='/logout') - Logout
      #home
        .posts
          nav.art-list
            ul.art-list-body
              each post, i in postlist
                li.art-list-item
                  .art-list-item-title-and-time
                    h2.art-list-title
                      h2 #{post.judul}
                    .art-list-time #{post._id.getTimestamp()}
                    a(href='/delete/#{post._id}', onclick="return confirmDelete()") Hapus
                  p
                    | #{post.konten}
      footer.footer
        .back-to-top
          a(href='#') Back to top
        // <div class="footer-nav"><p></p></div>
        .psi Ψ
        aside.offsite-links
          | PT. GITS Indonesia
    script(type='text/javascript', src='javascripts/fittext.js')
    script(type='text/javascript', src='javascripts/app.js')
    script(type='text/javascript', src='javascripts/respond.min.js')
    link(rel='stylesheet', type='text/css', href='stylesheets/screen.css')
    script(type='text/javascript').
      function confirmDelete() {
        var result = confirm('Apakah Anda yakin menghapus post ini?');
        if (result==true) {
          return true;
        }
        else {
          return false;
        }
      }
      var ga_ua = '{{! TODO: ADD GOOGLE ANALYTICS UA HERE }}';
      (function(g,h,o,s,t,z){g.GoogleAnalyticsObject=s;g[s]||(g[s]=
      function(){(g[s].q=g[s].q||[]).push(arguments)});g[s].s=+new Date;
      t=h.createElement(o);z=h.getElementsByTagName(o)[0];
      t.src='//www.google-analytics.com/analytics.js';
      z.parentNode.insertBefore(t,z)}(window,document,'script','ga'));
      ga('create',ga_ua);ga('send','pageview');